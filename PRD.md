# WebMirror MVP 产品需求文档

## 1. 文档信息

| 字段     | 内容                                  |
| -------- | ------------------------------------- |
| 产品名称 | WebMirror（暂定）                     |
| 文档版本 | v0.1                                  |
| 文档状态 | 草案                                  |
| 创建日期 | 2026-07-18                            |
| 目标版本 | MVP                                   |
| 首发平台 | Chrome、Microsoft Edge，Windows 10/11 |

## 2. 产品概述

WebMirror 是一款浏览器插件及本地辅助工具，用于将用户当前打开且有权访问的静态网站，一键保存为可离线运行的高保真镜像。

产品不通过截图推测或重新生成页面，而是保存网站原始 HTML、CSS、JavaScript、字体、图片、音频、视频、WebGL 模型、纹理、WASM、Worker 及其他运行资源，并在本地重建兼容的 HTTP 运行环境。

MVP 的核心价值是：

- 一键启动，无需用户配置抓取规则。
- 在符合条件的网站上，P90 在 120 秒内生成可运行镜像。
- 尽可能保留原网站的视觉、动画、音频和前端交互。
- 默认本地处理，不上传页面内容、Cookie、Token 或用户数据。
- 输出可独立保存、预览和再次打开的离线镜像。

## 3. 背景与问题

现有浏览器“保存网页”功能主要适用于普通 HTML 页面，对以下内容支持不足：

- JavaScript 动态加载的资源。
- WebGL、Canvas、Three.js 或自定义三维引擎。
- GLB、DDS、WASM、Worker、音频和字幕资源。
- CSS、JSON 或压缩 JavaScript 中间接引用的资源。
- 依赖正确 MIME、Range、CORS 或 HTTP Origin 的页面。
- 需要点击、滚动、拖拽后才进入的交互状态。

开发者、设计团队和内容所有者在页面归档、迁移、演示、QA 和灾备时，需要一种比截图更完整、比人工迁移更快速的离线保存方式。

## 4. 产品定位

### 4.1 产品定义

获得授权的静态网站高保真离线镜像工具。

### 4.2 核心原则

1. 保存原运行时，不在抓取关键路径上重新生成代码。
2. 快速可用优先，深度验证在后台继续。
3. 本地优先，默认不上传抓取内容。
4. 单页面优先，整站遍历不进入 MVP。
5. 明确展示完整、部分完成和外部依赖，不伪装成功。

### 4.3 非产品目标

MVP 不提供：

- 登录、支付、订单、数据库和管理后台的复制。
- 对访问控制、验证码、DRM、反爬虫或付费墙的绕过。
- 任意网站的服务端业务复制。
- 自动生成结构清晰的 React、Vue 或 Three.js 工程。
- 无限制整站爬取。
- 对不同浏览器、操作系统和 GPU 的逐像素绝对一致承诺。
- 未经授权的网站再发布、冒充或钓鱼能力。

## 5. 目标用户

### 5.1 主要用户

- 前端及创意开发者。
- 设计、互动和数字体验工作室。
- 网站所有者及内部技术团队。
- QA、视觉回归和网页归档人员。

### 5.2 主要场景

- 保存即将下线的活动网站。
- 将静态创意页面迁移到新的托管环境。
- 保存 WebGL、Canvas 和互动作品作为离线演示。
- 创建视觉和交互回归基线。
- 对客户授权页面进行本地备份。

## 6. MVP 范围

### 6.1 支持范围

- Chromium 内核的 Chrome 和 Microsoft Edge。
- Windows 10 和 Windows 11。
- 用户当前打开的单个页面。
- 公开页面或用户已获得访问及复制授权的页面。
- 同源静态资源和无需额外认证的公共 CDN 资源。
- HTML、CSS、JavaScript、JSON、图片、字体、音频和普通视频。
- GLB、GLTF、DDS、BIN、WASM、Worker、字幕和常见 WebGL 资源。
- 普通 DOM 页面、静态 SPA 以及客户端 WebGL 页面。
- 本地 HTTP 预览、镜像目录和 ZIP 导出。

### 6.2 性能适用范围

“P90 小于 120 秒”的性能目标适用于：

- 单页面。
- 总资源体积不超过 50MB。
- 总网络资源不超过 500 个。
- 单个资源不超过 20MB。
- 不包含大型流媒体、DRM 或持续下载。
- 网络有效带宽不低于 20Mbps。
- 资源服务器不存在明显限速或长时间错误重试。

超出范围时，产品继续尝试抓取，但不承诺两分钟完成。

## 7. 产品模式

### 7.1 极速镜像模式

MVP 默认且唯一的正式抓取模式。

- 保存原始页面代码和资源。
- 优先生成首屏预览和可运行本地版本。
- 保留原始目录关系和运行逻辑。
- 不执行代码格式化、框架转换和图片重编码。
- 快速验证完成后即可交付。

### 7.2 深度验证

作为极速镜像后的后台任务执行。

- 检查资源缺失、控制台错误和空白 Canvas。
- 回放可识别的点击、滚动、拖拽和页面入口操作。
- 对关键状态执行截图和感知相似度比较。
- 生成完整性报告。

### 7.3 工程重构模式

不进入 MVP。后续版本可将镜像转换为可维护工程，但不得与“高保真镜像”共用性能承诺。

## 8. 用户流程

1. 用户打开目标页面并等待页面可正常访问。
2. 用户点击浏览器工具栏中的 WebMirror 图标。
3. 插件展示来源域名、授权提示和“一键镜像”主按钮。
4. 用户点击“一键镜像”。
5. 系统执行预检查，识别页面类型、资源规模和明显的不支持项。
6. 系统接入当前标签页，在监听网络后自动刷新一次页面。
7. 系统发现、下载并本地化资源。
8. 15 秒内尽量展示首屏截图和实时进度。
9. 本地镜像可运行后，系统立即展示结果页。
10. 深度验证在后台继续，并更新完整性状态。
11. 用户可以打开本地镜像、打开目录、导出 ZIP 或查看报告。

## 9. 页面与状态

### 9.1 插件弹窗

- 当前页面标题和域名。
- 当前页面是否适合极速镜像。
- “一键镜像”主按钮。
- 输出目录设置入口。
- 本地辅助程序连接状态。
- 授权和隐私提示。

### 9.2 进度页

必须展示以下阶段，不展示虚假百分比：

- 正在检查页面。
- 正在监听并重新加载。
- 正在发现资源。
- 正在下载资源。
- 正在生成本地镜像。
- 正在快速验证。
- 正在深度验证。

同时展示：

- 已发现资源数。
- 已完成资源数。
- 已下载体积。
- 已用时间。
- 当前警告数量。

### 9.3 结果状态

| 状态     | 定义                                 |
| -------- | ------------------------------------ |
| 完整     | 本地运行成功，无缺失资源，无阻断错误 |
| 部分完成 | 本地可运行，但存在缺失资源或在线依赖 |
| 失败     | 无法生成可运行入口或发生阻断错误     |
| 已取消   | 用户主动终止                         |

### 9.4 结果页

- 打开本地镜像。
- 打开输出目录。
- 导出 ZIP。
- 重新验证。
- 重试失败资源。
- 查看完整性报告。
- 查看仍然存在的在线依赖。

## 10. 功能需求

### FR-001 当前标签页抓取

优先级：P0

- 用户只能对当前主动标签页启动抓取。
- 插件在用户明确点击后申请必要权限。
- 插件不得后台批量抓取浏览历史中的页面。

### FR-002 预检查

优先级：P0

系统应在正式刷新前识别：

- 页面 URL、Origin 和标题。
- HTML、Canvas、WebGL、iframe、Worker 和 Service Worker 使用情况。
- 已观察到的资源数量和大致体积。
- WebSocket、流媒体、DRM、登录态接口和持续请求。
- 页面是否明显超出性能适用范围。

预检查不得阻止用户尝试，但必须提示可能超时或不完整。

### FR-003 CDP 网络监听

优先级：P0

- 在刷新页面前启用 Network、Page、Runtime 和 Target 相关监听。
- 自动接入可观察的 iframe 和 Worker。
- 记录请求 URL、Method、状态码、重定向、MIME、Header、大小和来源类型。
- 在资源完成后建立可下载资源清单。
- 识别并忽略 Analytics、广告、遥测和无关 Beacon。
- 长连接不得阻止静态资源发现阶段结束。

### FR-004 资源发现

优先级：P0

资源发现必须组合使用：

- 页面真实网络请求。
- HTML 的 `src`、`href`、`srcset`、`poster` 和 preload。
- CSS 的 `url()` 和 `@import`。
- JSON 及资源清单。
- JavaScript AST 中可确定的静态 URL 字符串。
- Worker、WASM、GLB、DDS、BIN、字体、音频和字幕引用。
- Service Worker CacheStorage 中可安全读取的资源。

不得仅通过文件扩展名判断资源类型。

### FR-005 抓取完成判断

优先级：P0

默认结束条件：

- DOM 已进入可用状态。
- 普通网络请求已完成。
- 连续 2 秒没有发现新的有效资源。
- 忽略 WebSocket、EventSource、Analytics 和持续心跳。
- 默认发现阶段上限为 30 秒。

达到上限后应继续处理已发现资源，并将未完成项写入报告。

### FR-006 资源下载

优先级：P0

- 资源发现后立即进入下载队列，不等待全部发现完成。
- 每个 Origin 默认保持 8 个并发任务，可在 4 至 16 之间动态调整。
- 遇到 429、503 或明显限速时自动降低并发。
- 支持断点续传或失败资源单独重试。
- 资源写入时同步计算 SHA-256。
- 大文件必须流式落盘，不得全部保存在插件内存中。

### FR-007 全局缓存

优先级：P1

- 使用内容 Hash 进行跨任务资源去重。
- 保存 URL、ETag、Last-Modified、MIME、大小和 Hash 映射。
- 相同内容不得重复占用磁盘。
- 用户可以清理全部缓存或按时间清理。

### FR-008 路径本地化

优先级：P0

- 优先保留原网站目录结构。
- HTML、CSS 和 JSON 必须使用结构化解析器处理。
- JavaScript 只修改可确定的资源 URL，不得对整个压缩文件执行盲目正则替换。
- 支持相对路径、根路径、同源绝对路径和公共 CDN 路径。
- 无法离线化的外部请求必须记录为在线依赖。

### FR-009 本地 HTTP 运行环境

优先级：P0

- 使用 `127.0.0.1` 随机可用端口提供预览。
- 正确返回 MIME、Range、CORS、字体、WASM 和缓存 Header。
- 支持 SPA History API 回退。
- 支持 GLB、DDS、BIN、音频和普通视频资源。
- 用户关闭预览后，后台服务可以继续由本地辅助程序管理。

### FR-010 快速验证

优先级：P0

快速验证必须检查：

- 页面入口返回成功。
- HTML 和主脚本正常加载。
- 无本地资源 404。
- 无阻断级 JavaScript 错误。
- Canvas 或 WebGL 场景不是全空白。
- 首屏截图已生成。
- 本地运行时没有意外访问原站资源。

### FR-011 深度验证

优先级：P1

- 支持 DOM 可识别按钮和链接的声明式操作回放。
- 支持滚动、点击、拖拽和按键动作。
- Canvas 内部的通用语义理解不作为 MVP 验收项。
- 允许为 WebGL 站点配置少量站点适配器和动作脚本。
- 对关键状态保存截图并执行感知相似度比较。
- 不同 GPU 环境使用容差，不要求逐像素完全一致。

### FR-012 输出结构

优先级：P0

每次任务生成独立目录：

```text
mirror-output/
├─ site/                 # 可运行镜像
├─ assets/               # 内容寻址缓存引用或本地资源
├─ screenshots/          # 原站与本地验证截图
├─ mirror.json           # 资源及任务清单
├─ validation.json       # 验证结果
├─ report.html           # 用户可读报告
└─ launch.cmd            # 本地启动入口
```

### FR-013 任务清单

优先级：P0

`mirror.json` 至少包含：

- 清单版本。
- 来源 URL 和 Origin。
- 抓取时间。
- 浏览器版本、视口和设备像素比。
- 总资源数和总体积。
- 每个资源的原 URL、本地路径、MIME、状态、大小和 Hash。
- 忽略资源及忽略原因。
- 在线依赖。
- 抓取耗时分解。
- 最终状态。

### FR-014 数据与隐私

优先级：P0

默认不得保存或上传：

- Cookie。
- Authorization Header。
- 密码及表单输入值。
- LocalStorage 和 SessionStorage。
- 浏览器历史。
- 用户身份 Token。
- 精确的个人浏览行为。

需要 Cookie 才能重新请求的资源，应优先使用浏览器已获得的响应体；无法安全保存时标记为不支持，不得将 Cookie 写入镜像。

### FR-015 取消和清理

优先级：P0

- 用户可在任意阶段取消。
- 取消后停止新下载并结束验证。
- 已完成资源可以保留在全局缓存。
- 未完成的任务目录必须标记为中断，不得显示为成功。

## 11. 非功能需求

### 11.1 性能

| 指标                | MVP 目标             |
| ------------------- | -------------------- |
| 插件操作响应        | 小于 200ms           |
| 首屏截图预览        | P50 小于 15 秒       |
| 可运行镜像          | P50 小于 60 秒       |
| 可运行镜像          | P90 小于 120 秒      |
| 快速验证            | 小于 30 秒           |
| 50MB 内任务峰值内存 | 本地辅助程序小于 1GB |

性能统计必须排除用户主动暂停、断网和资源服务器持续故障，但不得排除正常的失败重试。

### 11.2 可靠性

- 单个资源失败不得导致整个任务崩溃。
- 本地辅助程序崩溃后不得产生“完整”结果。
- 任务日志和资源清单必须可用于问题诊断。
- 同一输入在相同环境下应产生稳定的路径和 Hash。

### 11.3 兼容性

- 支持当前稳定版及前两个主要版本的 Chrome 和 Edge。
- 本地辅助程序以独立安装包提供，用户无需单独安装 Node.js。
- MVP 首发仅保证 Windows 10/11。

### 11.4 安全

- 本地预览服务只监听 `127.0.0.1`。
- 每个预览任务使用不可预测的访问令牌或独立端口。
- 禁止页面通过本地服务访问任意本机文件。
- 输出目录必须限制路径穿越。
- 安装包和可执行文件必须进行代码签名。
- 不提供绕过访问控制和反爬虫的功能。

### 11.5 可用性

- 主流程只包含一个主要操作按钮。
- 用户不需要理解 CDP、CORS、MIME 或资源重写。
- 错误提示必须说明影响和可执行的下一步。
- 部分完成时必须明确列出缺失项，不得只展示笼统警告。

## 12. 技术架构

```text
Chrome/Edge Extension
├─ Popup UI
├─ Capture Controller
├─ CDP Session Manager
├─ Network Recorder
├─ Resource Discovery
└─ Progress Bridge
           │
           │ Native Messaging，仅传控制消息和元数据
           ▼
Local Helper
├─ Download Scheduler
├─ Content Cache
├─ Mirror Manifest
├─ HTML/CSS/JSON Rewriter
├─ Controlled JS URL Rewriter
├─ Local Preview Server
├─ Validation Runner
└─ Exporter
```

### 12.1 技术选择

- 插件：Chrome Manifest V3、TypeScript。
- UI：轻量前端方案，避免引入大型运行时。
- 本地辅助程序：MVP 使用打包后的 Node.js/TypeScript，后续按性能需要评估 Rust。
- HTML 解析：标准 DOM 或成熟 HTML Parser。
- CSS 解析：PostCSS 级别的结构化解析。
- JavaScript 解析：SWC、Babel Parser 或同等级 AST 工具。
- 本地服务：支持 MIME、Range、CORS 和 History Fallback 的轻量 HTTP Server。
- 验证：Playwright。
- 缓存：SQLite 元数据加内容寻址文件目录。

## 13. 性能优化策略

1. 资源发现与下载并行执行。
2. HTML、CSS、主脚本和首屏资源进入高优先级队列。
3. 音频、后续场景和非首屏模型进入普通队列。
4. 复用浏览器已获取的响应，避免无必要的二次下载。
5. 公共静态资源由本地辅助程序直接并发下载。
6. 下载、Hash 和落盘使用流式管道。
7. 图片、音频、视频和压缩模型不在关键路径重新压缩。
8. ZIP 导出在镜像可运行后异步执行。
9. Playwright 浏览器池和本地服务保持预热。
10. 仅重试失败资源，不重新启动完整任务。
11. 使用 SHA-256 内容缓存消除跨页面重复资源。
12. 通过阶段耗时埋点持续定位慢请求和串行瓶颈。

## 14. 完整性评分

结果页展示 0 至 100 的完整性评分，但必须同时展示具体问题。

建议权重：

| 项目                   | 权重 |
| ---------------------- | ---: |
| HTML 和主程序可运行    |   25 |
| 静态资源完整           |   25 |
| 无阻断 JavaScript 错误 |   15 |
| 无意外在线依赖         |   15 |
| 首屏视觉相似           |   10 |
| 关键交互验证通过       |   10 |

Canvas、视频、随机动画和实时内容不得仅依赖像素差异扣分。

## 15. 验收标准

### 15.1 基础功能

- 用户可以从浏览器工具栏一键启动任务。
- 插件能够刷新并监听当前页面的完整网络加载过程。
- 输出目录可以通过本地 HTTP 服务成功运行。
- 任务报告可以追踪每个资源的来源和状态。
- 取消、失败、部分完成和完整状态区分明确。

### 15.2 离线要求

断开外网后：

- 本地页面入口仍可打开。
- 已声明支持的图片、字体、音频、脚本和 WebGL 资源仍可加载。
- 不产生对原站静态资源的请求。
- Analytics 和遥测请求不影响页面运行。
- 外部业务接口必须被禁用或明确标记为在线依赖。

### 15.3 性能要求

在标准测试环境中，对满足性能适用范围的测试集：

- P50 可运行镜像时间小于 60 秒。
- P90 可运行镜像时间小于 120 秒。
- 任务成功后本地资源 404 数量为 0。
- 单个失败资源可以独立重试。

标准测试环境：

- Windows 11。
- 当前稳定版 Chrome。
- 8 核 CPU、16GB 内存。
- 20Mbps 以上稳定网络。
- 冷启动和热缓存分别统计。

### 15.4 WebGL 基准

内部基准页面包含 `https://2019.makemepulse.com/` 或获得授权的等价副本。

验收要求：

- 入口、Start、Next 和首个 Drag 场景可以运行。
- GLB、DDS、BIN、字体和音频无本地 404。
- Canvas 不为空白。
- 音频在用户点击后可以正常解锁播放。
- 本地页面无对原站静态资源的依赖。
- 可运行镜像在性能适用环境下 120 秒内完成。

不得在未获得授权时分发该基准页面的镜像内容。

## 16. 核心指标

### 16.1 产品指标

- 一键任务启动率。
- 可运行镜像成功率。
- 完整结果占比。
- 部分完成结果占比。
- 用户打开本地镜像的比例。
- 失败资源重试成功率。

### 16.2 工程指标

- 总耗时 P50、P90、P99。
- 页面发现耗时。
- 下载耗时及有效吞吐。
- 路径处理耗时。
- 快速验证耗时。
- 平均资源数和总体积。
- 缓存命中率。
- 本地 404 数量。
- 意外外网请求数量。
- 插件和本地辅助程序崩溃率。

## 17. 风险与应对

| 风险                        | 影响                 | 应对方案                                       |
| --------------------------- | -------------------- | ---------------------------------------------- |
| 交互后才出现隐藏资源        | 离线缺失             | 网络监听、静态分析、深度验证、站点适配器       |
| 压缩 JS 中硬编码绝对 URL    | 本地仍访问原站       | AST 定向替换、Origin 映射、在线依赖报告        |
| CORS、MIME 或 Range 不兼容  | 模型、字体或视频失败 | 本地服务器复刻必要 Header 并增加专项测试       |
| WebGL 在不同 GPU 上结果不同 | 视觉误报             | 感知相似度和 Canvas 非空检查，不要求逐像素一致 |
| `debugger` 权限影响商店审核 | 上架延期             | 单一用途、用户触发、最小权限、完整隐私说明     |
| 页面包含敏感登录数据        | 隐私风险             | 默认过滤 Cookie、Token、表单值和浏览器存储     |
| 用户用于未授权复制          | 法务和平台风险       | 授权确认、使用条款、禁止绕过和再发布定位       |
| 大型媒体页面超时            | 用户预期落差         | 预检查体积、明确预计时间、部分完成和后台继续   |

## 18. 里程碑

### 第 1 至 2 周：抓取核心

- MV3 插件骨架。
- 本地辅助程序通信。
- CDP 网络监听。
- 当前页面刷新和资源清单。
- 基础任务进度。

### 第 3 至 4 周：离线镜像

- 并发下载和流式落盘。
- HTML、CSS、JSON 路径处理。
- 本地 HTTP 服务。
- `mirror.json`。
- 基础缓存。

### 第 5 周：验证与报告

- Playwright 快速验证。
- 404 和控制台错误检测。
- 首屏截图。
- 完整性报告。

### 第 6 周：WebGL 基准

- GLB、DDS、BIN、音频和 Worker 支持。
- Canvas 非空检测。
- Start、Next、Drag 动作脚本。
- WebGL 基准性能优化。

### 第 7 周：可靠性和安全

- 取消、重试和崩溃恢复。
- Token、Cookie 和表单数据过滤。
- 路径穿越和本地服务器安全测试。
- 安装包和代码签名。

### 第 8 周：Beta

- Chrome 和 Edge 兼容测试。
- 20 个不同类型页面的基准测试。
- 隐私政策、使用条款和商店材料。
- 小范围授权用户测试。

## 19. 发布门槛

满足以下条件后方可进入公开 Beta：

- 20 个基准页面中至少 18 个生成可运行镜像。
- 符合性能范围的页面 P90 小于 120 秒。
- 完整任务的本地资源 404 为 0。
- 未发现 Cookie、Token、密码或表单值写入输出。
- 本地服务通过路径穿越和任意文件读取测试。
- Chrome 和 Edge 当前稳定版通过核心流程测试。
- 安装、更新和卸载流程可用。
- 隐私政策和授权使用说明完成。

## 20. 已确定决策

- MVP 只做极速镜像，不做工程重构。
- Chrome 和 Edge 共享 Chromium 核心实现。
- 首版使用插件加本地辅助程序的混合架构。
- 默认本地处理，不依赖云端抓取服务。
- 首版只支持 Windows。
- 首版聚焦单页面，不做整站递归。
- 快速验证阻塞交付，深度验证后台执行。
- 两分钟是适用范围内的 P90 指标，不是任意页面的绝对承诺。

## 21. 待确认事项

- 正式产品名称。
- 首发时 Chrome 和 Edge 是否同时上架。
- 输出是否默认生成 ZIP，或仅在用户点击后生成。
- 是否在 Beta 阶段提供简单的同域多页面队列。
- 商业版是否增加云端同步和团队镜像库。
- macOS 版本进入哪个后续里程碑。
