# WebMirror MVP 项目实施计划

## 1. 文档信息

| 字段             | 内容               |
| ---------------- | ------------------ |
| 关联 PRD         | [PRD.md](./PRD.md) |
| 计划版本         | v1.0               |
| 计划状态         | 待评审             |
| 制定日期         | 2026-07-18         |
| 基准启动日期     | 2026-07-20         |
| 功能完成目标     | 2026-09-11         |
| 公开 Beta 目标   | 2026-10-09         |
| 正式发布候选目标 | 2026-11-20         |

## 2. 执行摘要

本计划将 PRD 中的 8 周粗粒度里程碑，扩展为风险驱动、可验证、可逐步交付的项目实施方案。

整体安排为：

- 第 1 周完成关键技术风险验证。
- 第 2 至 8 周完成端到端产品能力和功能冻结。
- 第 9 至 12 周完成 WebGL、性能、安全、可靠性和公开 Beta。
- Beta 后安排 6 周真实样本硬化，再决定正式发布。

8 周是“功能完成”目标，不是“商业稳定版”目标。浏览器权限、本地辅助程序、恶意页面隔离、资源完整性和商店发布均需要单独的验证周期，不能在最后一周集中处理。

基准团队配置为 4 名全职工程人员，加 0.5 名产品/设计支持。若团队规模更小，应延长时间，不应通过删除安全、测试和离线验证环节维持日期。

## 3. 项目目标

### 3.1 Beta 目标

在 2026-10-09 前交付一个可安装、可更新、可诊断的 Windows Beta 版本，具备：

- Chrome 和 Edge 一键抓取当前页面。
- 插件与本地辅助程序稳定通信。
- CDP 网络监听、资源发现和敏感信息过滤。
- 并发下载、流式落盘、内容缓存和失败重试。
- HTML、CSS、JSON 和受控 JavaScript URL 本地化。
- 本地 HTTP 预览及离线运行。
- 快速验证、深度验证和完整性报告。
- WebGL、GLB、DDS、BIN、WASM、Worker、字体和音频支持。
- P50 小于 60 秒、P90 小于 120 秒的适用范围指标。
- 20 个基准页面中至少 18 个生成可运行镜像。

### 3.2 正式发布候选目标

在 2026-11-20 前完成：

- 至少 100 个授权真实页面的 Beta 数据验证。
- 适用范围页面成功率达到 95% 以上。
- 完整结果本地资源 404 为 0。
- 未出现敏感凭据写入输出的安全事件。
- 安装、更新、回滚和卸载流程通过测试。
- Chrome 和 Edge 发布材料、隐私政策、使用条款和支持流程就绪。

### 3.3 性能统计适用范围

P50 小于 60 秒、P90 小于 120 秒的指标只统计同时满足以下条件的页面：

- 当前单页面。
- 总资源体积不超过 50MB。
- 总网络资源不超过 500 个。
- 单个资源不超过 20MB。
- 网络有效带宽不低于 20Mbps。
- 不包含大型流媒体、DRM、持续下载或必须保持在线的业务接口。
- 资源服务器不存在持续限速或长时间故障。

超出范围的任务仍应完成预检查、尽力抓取并准确报告，但不得混入两分钟性能达标率。

## 4. 规划原则

### 4.1 先纵向闭环，再横向扩展

第一个可运行版本必须打通：

```text
点击插件
→ 获取当前页面
→ 记录资源
→ 保存资源
→ 本地启动
→ 断网验证
```

在该闭环完成前，不优先开发复杂 UI、完整性评分、深度交互遍历或多页面能力。

### 4.2 优先处理高风险未知项

最先验证：

- CDP 能否完整观察 WebGL 页面资源。
- 浏览器已加载响应如何安全交给本地辅助程序。
- 压缩 JavaScript 和绝对 URL 如何本地化。
- WebGL、音频和 Worker 是否可在本地 Origin 下运行。
- 本地预览服务如何隔离不可信页面。
- Native Messaging 安装和版本兼容是否稳定。

### 4.3 用阶段门替代日期驱动

阶段结束必须满足退出标准。仅到达日期但未满足质量门时，不得把阶段标记为完成。

### 4.4 性能先测量再优化

所有主要阶段必须记录耗时、吞吐、内存、资源数和失败数。不得在缺少基线数据时进行大规模重构。

### 4.5 安全和隐私默认开启

抓取页面属于不可信输入。安全约束必须进入架构和测试，不作为发布前的附加检查。

### 4.6 保持 MVP 边界

以下需求出现时默认进入后续版本：

- 多页面整站抓取。
- React、Vue 或 Three.js 工程重构。
- 云端同步。
- 团队素材库。
- macOS 和 Firefox。
- 登录、支付和后端接口复制。

## 5. 基准假设

### 5.1 团队配置

| 角色               |     人数 | 主要职责                             |
| ------------------ | -------: | ------------------------------------ |
| 技术负责人         |        1 | 架构、关键风险、代码评审、质量门     |
| 浏览器与抓取工程师 |        1 | MV3、CDP、Target、资源发现           |
| 本地引擎工程师     |        1 | 下载、缓存、重写、本地服务、安装包   |
| 前端与扩展工程师   |        1 | 插件 UI、任务状态、结果页、用户流程  |
| QA/自动化/安全     | 0.5 至 1 | 测试平台、Playwright、安全和发布验证 |
| 产品/UX            |      0.5 | 范围、交互、用语、Beta 反馈          |

技术负责人可以兼任浏览器或本地引擎方向，但关键模块至少需要第二人评审。

### 5.2 不同团队规模的时间参考

| 团队         | 可演示 POC |   公开 Beta | 正式发布候选 |
| ------------ | ---------: | ----------: | -----------: |
| 4 名工程人员 |  1 至 2 周 |       12 周 |        18 周 |
| 2 名工程人员 |  3 至 4 周 | 18 至 22 周 |  26 至 30 周 |
| 单人开发     |  4 至 6 周 | 28 至 36 周 |    40 周以上 |

### 5.3 技术环境

- TypeScript 作为主要开发语言。
- Chrome Manifest V3。
- 本地辅助程序使用可独立分发的 Node.js/TypeScript 可执行包。
- Monorepo 管理扩展、本地程序和共享协议。
- Playwright 负责端到端和视觉验证。
- SQLite 保存任务和缓存元数据。
- 内容寻址文件系统保存资源。

## 6. 项目工作流

项目拆分为六条并行工作流：

| 工作流       | 代码 | 范围                                       |
| ------------ | ---- | ------------------------------------------ |
| 产品与体验   | PX   | 用户流程、界面、错误提示、文档             |
| 扩展平台     | EXT  | MV3、权限、Popup、结果页、Native Messaging |
| 抓取引擎     | CAP  | CDP、网络事件、资源发现、敏感数据过滤      |
| 本地镜像引擎 | MIR  | 下载、缓存、重写、本地服务、导出           |
| 验证与质量   | VAL  | Playwright、截图、交互、报告、基准测试     |
| 安全与发布   | SEC  | 威胁模型、安装包、签名、商店、回滚         |

## 7. 架构边界

### 7.1 推荐仓库结构

```text
plugin/
├─ apps/
│  ├─ extension/              # Chrome/Edge 扩展
│  └─ helper/                 # 本地辅助程序
├─ packages/
│  ├─ protocol/               # 版本化通信协议和 Schema
│  ├─ job-model/              # 任务状态机和事件
│  ├─ capture-core/           # CDP 事件归一化
│  ├─ discovery/              # HTML/CSS/JS/JSON 资源发现
│  ├─ downloader/             # 下载调度、重试、流式落盘
│  ├─ cache/                  # SQLite 和内容寻址缓存
│  ├─ rewriter/               # 路径及 URL 本地化
│  ├─ preview-server/         # 本地 HTTP 运行环境
│  ├─ validator/              # 快速和深度验证
│  ├─ report/                 # mirror.json、报告和评分
│  └─ test-fixtures/          # 可控测试页面和资源
├─ tests/
│  ├─ integration/
│  ├─ e2e/
│  ├─ performance/
│  └─ security/
├─ docs/
│  ├─ adr/
│  ├─ runbooks/
│  └─ release/
├─ PRD.md
└─ PROJECT_PLAN.md
```

### 7.2 关键边界

- 扩展负责获取用户授权、接入标签页和产生事件。
- 本地辅助程序负责磁盘、网络下载、缓存、本地服务和验证。
- Native Messaging 只传控制消息、任务事件和元数据。
- 抓取到的页面代码不得在扩展 Origin 中执行。
- 预览页面和本地控制 API 必须运行在不同端口或不同访问令牌下。
- 所有跨进程消息使用版本化 JSON Schema 校验。

### 7.3 任务状态机

```text
CREATED
→ PREFLIGHT
→ ATTACHING
→ DISCOVERING
→ DOWNLOADING
→ LOCALIZING
→ STARTING_PREVIEW
→ FAST_VALIDATING
→ READY
→ DEEP_VALIDATING
→ COMPLETE | PARTIAL

任意运行状态
→ CANCELLING
→ CANCELLED

任意运行状态
→ FAILED
```

任务状态只能由统一状态机转换，UI 不得自行推断状态。

## 8. 交付节奏

### 8.1 迭代方式

- 使用两周一个 Sprint。
- 每周至少一次端到端演示。
- 每个 Sprint 结束执行阶段门检查。
- 每个高风险功能先提交短期技术验证，再进入正式实现。
- 每个任务必须绑定 PRD 要求、验收测试或风险项。

### 8.2 每周节奏

| 时间       | 活动                   | 目的               |
| ---------- | ---------------------- | ------------------ |
| 周一       | Sprint 规划和风险更新  | 明确本周输出和阻塞 |
| 周二至周四 | 开发、评审、自动化测试 | 完成纵向能力       |
| 周三       | 中期集成检查           | 提前发现跨模块问题 |
| 周五       | 演示、指标回顾、复盘   | 用运行结果判断进度 |

### 8.3 Definition of Ready

任务开始前必须具备：

- 对应 PRD 或风险编号。
- 清晰输入、输出和失败行为。
- 可验证的完成条件。
- 已识别依赖和安全影响。
- 复杂任务已有技术方案或 ADR。

### 8.4 Definition of Done

任务完成必须满足：

- 代码已合并并通过至少一人评审。
- 单元或集成测试覆盖主要成功和失败路径。
- 日志、错误码和任务事件完整。
- 不新增未说明的敏感数据处理。
- 文档和协议 Schema 已同步。
- 在 Chrome 和 Edge 的目标环境中通过相关测试。
- 对性能关键路径提供前后对比数据。

## 9. 总体时间表

### 9.1 阶段表

| 阶段                    | 日期                     | 目标                       |
| ----------------------- | ------------------------ | -------------------------- |
| P0 风险验证             | 2026-07-20 至 2026-07-24 | 证明方案可行，冻结关键架构 |
| P1 工程基础与最小闭环   | 2026-07-27 至 2026-08-07 | 简单页面一键离线运行       |
| P2 抓取与资源发现       | 2026-08-10 至 2026-08-21 | 建立稳定、可测试的捕获核心 |
| P3 下载、缓存与本地重放 | 2026-08-24 至 2026-09-04 | 形成可靠的离线镜像         |
| P4 产品体验与功能冻结   | 2026-09-07 至 2026-09-11 | 完成 8 周功能目标          |
| P5 验证与 WebGL         | 2026-09-14 至 2026-09-25 | 达成参考 WebGL 页面目标    |
| P6 安全、可靠性和性能   | 2026-09-28 至 2026-10-02 | 满足公开 Beta 质量门       |
| P7 发布准备与 Beta      | 2026-10-05 至 2026-10-09 | 发布授权用户 Beta          |
| P8 Beta 硬化            | 2026-10-12 至 2026-11-20 | 真实样本优化和正式发布决策 |

### 9.2 关键路径

```text
CDP 网络记录
→ 资源清单
→ 下载和缓存
→ URL 本地化
→ 本地 HTTP 重放
→ 断网验证
→ WebGL 验证
→ 安全和性能门
→ Beta
```

插件视觉完善、报告样式和 ZIP 导出不应阻塞关键路径，除非影响安装或核心用户流程。

## 10. P0 风险验证

日期：2026-07-20 至 2026-07-24

### 10.1 阶段目标

用最小代码证明核心架构可以支撑目标网站，并尽早暴露必须调整产品范围的问题。

### 10.2 具体任务

| 编号   | 任务                                            | 产出           | 负责人     |
| ------ | ----------------------------------------------- | -------------- | ---------- |
| P0-T01 | 建立 Monorepo、TypeScript、Lint、测试和 CI 骨架 | 可构建仓库     | 技术负责人 |
| P0-T02 | 建立第一版 ADR 清单                             | 架构决策记录   | 技术负责人 |
| P0-T03 | 创建 10 个基础测试页面和 3 个失败页面           | Fixture 集     | QA         |
| P0-T04 | 编写 MV3 扩展，点击后接入当前标签页             | 扩展 POC       | EXT/CAP    |
| P0-T05 | 监听页面刷新后的 Network、Page 和 Target 事件   | 原始事件日志   | CAP        |
| P0-T06 | 生成 URL、状态、MIME、大小和来源资源清单        | `capture.json` | CAP        |
| P0-T07 | 建立 Native Messaging 握手 POC                  | 双向心跳       | EXT/MIR    |
| P0-T08 | 将公共资源并发保存到临时目录                    | 下载 POC       | MIR        |
| P0-T09 | 启动最小本地 HTTP 服务                          | 本地入口       | MIR        |
| P0-T10 | 对简单页面完成断网重放                          | 离线演示       | 全体       |
| P0-T11 | 对授权 WebGL 基准完成入口和首场景重放           | WebGL 演示     | CAP/MIR    |
| P0-T12 | 完成第一版威胁模型                              | 风险清单       | SEC        |
| P0-T13 | 记录阶段耗时、内存和资源缺失                    | 性能基线       | QA         |

### 10.3 必须形成的 ADR

- ADR-001：为什么使用插件加本地辅助程序。
- ADR-002：Native Messaging 协议和版本协商。
- ADR-003：资源响应体获取策略。
- ADR-004：本地 URL 映射和 JavaScript 重写策略。
- ADR-005：预览服务和控制服务的安全隔离。
- ADR-006：内容寻址缓存目录结构。

### 10.4 阶段门

进入 P1 前必须满足：

- 点击扩展后可以记录当前页面刷新产生的主要资源。
- 简单测试页面可以在断网后本地运行。
- WebGL 基准至少完成入口和一个 Canvas 场景渲染。
- 扩展与本地辅助程序可以稳定握手和断线重连。
- 输出中不存在 Cookie、Authorization Header 和表单值。
- 已明确列出无法解决或需要缩小范围的风险。

### 10.5 停止或调整条件

出现以下情况必须重新评审架构：

- WebGL 基准的模型或纹理无法在本地 Origin 下加载。
- 无法在不保存凭据的前提下获得必要公开资源。
- Native Messaging 无法可靠承载任务控制和进度事件。
- 本地预览页面可以访问辅助程序的任意文件或控制接口。

## 11. P1 工程基础与最小闭环

日期：2026-07-27 至 2026-08-07

### 11.1 阶段目标

从 POC 代码转为可持续开发的端到端产品骨架，实现普通静态页面一键生成离线镜像。

### 11.2 具体任务

| 编号   | 任务                               | 产出             | 负责人     |
| ------ | ---------------------------------- | ---------------- | ---------- |
| P1-T01 | 实现版本化 Extension-Helper 协议   | `protocol` 包    | EXT/MIR    |
| P1-T02 | 实现任务状态机和合法转换校验       | `job-model` 包   | 技术负责人 |
| P1-T03 | 实现结构化日志和任务关联 ID        | 日志规范         | MIR        |
| P1-T04 | 实现插件 Popup 最小流程            | 一键入口         | EXT        |
| P1-T05 | 实现辅助程序安装检测和版本提示     | 连接状态         | EXT/MIR    |
| P1-T06 | 实现任务创建、进度、取消和失败状态 | 完整任务生命周期 | EXT/MIR    |
| P1-T07 | 实现基础资源清单 Schema            | `mirror.json` v1 | CAP        |
| P1-T08 | 实现基础下载队列                   | 下载器 v1        | MIR        |
| P1-T09 | 实现 HTML 和 CSS 基础路径处理      | 重写器 v1        | MIR        |
| P1-T10 | 实现本地预览启动和关闭             | 预览服务 v1      | MIR        |
| P1-T11 | 建立一键 E2E 测试                  | 简单页面闭环测试 | QA         |
| P1-T12 | 建立开发版安装和调试脚本           | 开发环境文档     | SEC/EXT    |

### 11.3 工程规范

- 所有协议消息使用 JSON Schema 验证。
- 所有任务事件必须包含任务 ID、时间、阶段和错误码。
- 扩展和辅助程序版本不兼容时禁止启动抓取。
- 任务输出写入临时目录，只有成功进入 READY 后才原子化改名。
- 不允许 UI 直接读写辅助程序内部数据库。

### 11.4 测试要求

- 状态机全部合法和非法转换测试。
- Native Messaging 断开、重连和版本不兼容测试。
- 取消时停止新下载和清理临时目录测试。
- HTML/CSS 路径重写单元测试。
- 简单页面断网 E2E 测试。

### 11.5 阶段门

- 新用户可以安装开发版扩展和辅助程序。
- 一个简单静态页面可以从点击到本地 READY。
- 任务取消、失败和完成状态准确。
- 断开外网后页面仍可打开，且无原站静态资源请求。
- CI 可以在干净环境中构建扩展和辅助程序。

## 12. P2 抓取与资源发现

日期：2026-08-10 至 2026-08-21

### 12.1 阶段目标

建立可扩展、可重放、可测试的捕获核心，覆盖普通 DOM、SPA、iframe、Worker 和常见静态 WebGL 资源。

### 12.2 CDP 会话任务

| 编号   | 任务                                           | 产出                |
| ------ | ---------------------------------------------- | ------------------- |
| P2-T01 | 实现标签页 attach、detach 和冲突检测           | CDP Session Manager |
| P2-T02 | 自动接入可观察 iframe、Worker 和子 Target      | Target Manager      |
| P2-T03 | 归一化请求、重定向、缓存命中和完成事件         | Network Event Model |
| P2-T04 | 处理请求失败、取消和超时                       | Failure Model       |
| P2-T05 | 区分普通请求、WebSocket、EventSource 和 Beacon | Resource Classifier |
| P2-T06 | 记录响应 Header、MIME、大小和来源链路          | Resource Metadata   |

### 12.3 资源发现任务

| 编号   | 任务                                   | 产出                 |
| ------ | -------------------------------------- | -------------------- |
| P2-T07 | HTML 属性资源发现                      | HTML Discoverer      |
| P2-T08 | CSS `url()` 和 `@import` 发现          | CSS Discoverer       |
| P2-T09 | JSON 清单资源发现                      | JSON Discoverer      |
| P2-T10 | JavaScript AST 静态 URL 发现           | JS Discoverer        |
| P2-T11 | Worker、WASM、GLB、DDS、BIN 和音频分类 | Binary Discoverer    |
| P2-T12 | Service Worker CacheStorage 发现策略   | CacheStorage Adapter |
| P2-T13 | Blob、Data URL 和内联资源处理策略      | Inline Asset Model   |
| P2-T14 | 资源去重和来源证据合并                 | Discovery Graph      |

### 12.4 完成条件算法

实现以下组合判断：

- DOM 已可用。
- 普通请求计数为零。
- 连续 2 秒没有新增有效资源。
- 长连接和遥测不参与静默判断。
- 默认最多发现 30 秒。
- 对正在下载的大文件允许进入后续处理，不错误判定超时。

算法必须可配置并记录“为什么结束”。

### 12.5 隐私过滤

- 请求 Header 在进入任务模型前过滤。
- Cookie、Authorization、Proxy-Authorization 和敏感自定义 Header 不写入磁盘。
- URL 查询参数默认保留以确保资源可用，但报告中进行脱敏显示。
- 表单值、LocalStorage 和 SessionStorage 不进入抓取流程。
- 错误日志不得打印完整 Token 或签名 URL。

### 12.6 测试要求

Fixture 必须覆盖：

- 多级 CSS import。
- srcset 和 preload。
- 重定向和缓存命中。
- iframe 和 Worker。
- Service Worker 缓存。
- WebSocket 不阻塞完成。
- 404、403、超时和取消。
- URL 中包含空格、Unicode、查询参数和 Fragment。
- 同一资源由多个来源发现。
- 敏感 Header 和签名 URL 脱敏。

### 12.7 阶段门

- 资源清单对基础 Fixture 的已知资源召回率达到 100%。
- 资源清单不保存禁止的敏感字段。
- iframe 和 Worker 资源可以归入同一任务。
- WebSocket 页面可以在 30 秒内结束发现阶段。
- 捕获核心有可重复的集成测试，不依赖公网。

## 13. P3 下载、缓存与本地重放

日期：2026-08-24 至 2026-09-04

### 13.1 阶段目标

把资源清单稳定转化为可离线运行的本地镜像，形成产品的主要技术价值。

### 13.2 下载系统

| 编号   | 任务                           | 产出                 |
| ------ | ------------------------------ | -------------------- |
| P3-T01 | 实现按 Origin 分组的优先级队列 | Scheduler            |
| P3-T02 | 实现 4 至 16 的自适应并发      | Adaptive Concurrency |
| P3-T03 | 实现流式下载、Hash 和原子落盘  | Download Pipeline    |
| P3-T04 | 实现指数退避和 `Retry-After`   | Retry Policy         |
| P3-T05 | 实现大文件断点续传             | Resume Support       |
| P3-T06 | 实现磁盘空间预检查             | Capacity Guard       |
| P3-T07 | 实现失败资源单独重试           | Retry UI API         |

### 13.3 内容缓存

| 编号   | 任务                               | 产出              |
| ------ | ---------------------------------- | ----------------- |
| P3-T08 | 设计 SHA-256 内容目录              | CAS Layout        |
| P3-T09 | 建立 SQLite URL、Header、Hash 映射 | Cache Database    |
| P3-T10 | 实现 ETag 和 Last-Modified 复用    | Conditional Fetch |
| P3-T11 | 实现缓存引用计数和清理             | Cache GC          |
| P3-T12 | 实现损坏缓存检测                   | Integrity Check   |

### 13.4 本地化和重写

| 编号   | 任务                         | 产出              |
| ------ | ---------------------------- | ----------------- |
| P3-T13 | HTML 结构化 URL 重写         | HTML Rewriter     |
| P3-T14 | CSS 结构化 URL 重写          | CSS Rewriter      |
| P3-T15 | JSON 资源清单重写            | JSON Rewriter     |
| P3-T16 | JavaScript 静态 URL 定向替换 | JS URL Rewriter   |
| P3-T17 | 同源、根路径和 CDN 路径映射  | URL Map           |
| P3-T18 | 生成在线依赖清单             | Dependency Report |
| P3-T19 | 禁用 Analytics 和非必要遥测  | Telemetry Filter  |

### 13.5 本地 HTTP 服务

- 只绑定 `127.0.0.1`。
- 支持正确 Content-Type。
- 支持 Range 请求。
- 支持字体和 WASM MIME。
- 支持 SPA History Fallback。
- 支持 Cache-Control 和条件请求。
- 不暴露辅助程序控制 API。
- 不允许通过路径穿越读取镜像目录外文件。

### 13.6 离线测试

所有集成测试必须包含：

1. 抓取页面。
2. 阻断全部公网网络。
3. 启动本地镜像。
4. 记录所有本地和外部请求。
5. 确认本地资源无 404。
6. 确认未声明的外部请求为零。

### 13.7 阶段门

- 10 个基础页面全部可以断网运行。
- 完整结果本地资源 404 为 0。
- 失败资源可以单独重试并更新清单。
- 50MB 任务峰值内存小于 1GB。
- 缓存命中时不会重复写入相同内容。
- 路径穿越和任意文件读取测试失败率为 0。

## 14. P4 产品体验与功能冻结

日期：2026-09-07 至 2026-09-11

### 14.1 阶段目标

将技术闭环整理成普通用户可理解的一键流程，并冻结 Beta 范围。

### 14.2 具体任务

| 编号   | 任务                               | 产出           |
| ------ | ---------------------------------- | -------------- |
| P4-T01 | 完成预检查界面                     | 页面适用性提示 |
| P4-T02 | 完成一键镜像主流程                 | 主按钮和确认   |
| P4-T03 | 完成阶段化进度展示                 | 非虚假进度     |
| P4-T04 | 完成完整、部分、失败和取消状态     | 结果状态       |
| P4-T05 | 完成打开镜像、目录和报告操作       | 结果操作       |
| P4-T06 | 完成重试失败资源                   | 局部重试       |
| P4-T07 | 完成输出目录和缓存设置             | 设置页         |
| P4-T08 | 完成辅助程序未安装和版本不兼容引导 | 安装引导       |
| P4-T09 | 完成授权、隐私和限制说明           | 合规用语       |
| P4-T10 | 完成键盘访问和基础无障碍检查       | UX 质量        |

### 14.3 体验原则

- 默认只展示一个主要操作。
- 不向用户展示 CDP、CORS、MIME 等实现术语。
- 进度显示阶段、数量、体积和时间，不使用无法计算的百分比。
- 部分完成必须列出具体缺失资源和影响。
- 超过性能适用范围时提前说明，不在超时后才提示。

### 14.4 功能冻结规则

2026-09-11 后：

- 不新增 P0 产品能力。
- 新需求必须替换同等工作量，不能直接扩大范围。
- 允许修复缺陷、完善安全、性能和 WebGL 支持。
- 多页面、云端和工程重构统一进入后续版本。

### 14.5 阶段门

- 5 名内部测试用户可以在不解释技术概念的情况下完成抓取。
- 用户能够区分完整、部分和失败结果。
- 所有主流程可以取消或恢复到稳定状态。
- 功能范围与 PRD 一致，未引入多页面和工程重构。

## 15. P5 验证与 WebGL

日期：2026-09-14 至 2026-09-25

### 15.1 阶段目标

建立可信的验证系统，并达到授权 WebGL 基准页面的验收要求。

### 15.2 快速验证

| 编号   | 任务                         | 产出                     |
| ------ | ---------------------------- | ------------------------ |
| P5-T01 | 建立预热 Playwright 浏览器池 | Validator Runtime        |
| P5-T02 | 检查入口状态和主脚本         | Startup Check            |
| P5-T03 | 捕获本地 404 和失败请求      | Network Check            |
| P5-T04 | 捕获阻断级控制台错误         | Console Check            |
| P5-T05 | 检查 Canvas 非空             | Canvas Pixel Check       |
| P5-T06 | 生成首屏截图                 | Screenshot Baseline      |
| P5-T07 | 检查意外原站请求             | Offline Dependency Check |

### 15.3 深度验证

| 编号   | 任务                                | 产出              |
| ------ | ----------------------------------- | ----------------- |
| P5-T08 | 建立声明式动作脚本格式              | Action Recipe     |
| P5-T09 | 支持点击、滚动、按键和拖拽          | Action Runner     |
| P5-T10 | 建立 DOM 入口按钮发现               | Entry Heuristics  |
| P5-T11 | 建立站点适配器接口                  | Adapter API       |
| P5-T12 | 建立关键状态截图                    | Checkpoints       |
| P5-T13 | 实现感知相似度及动态区域容差        | Visual Comparator |
| P5-T14 | 生成 `validation.json` 和 HTML 报告 | Validation Report |

### 15.4 WebGL 专项

- GLB、GLTF、DDS、BIN 和 WASM MIME 验证。
- Worker 加载和相对路径验证。
- WebGL Context 创建和丢失检测。
- Canvas 非空和连续帧变化检测。
- 用户点击后音频解锁验证。
- Range 请求验证。
- Start、Next 和首个 Drag 动作脚本。
- 不同 GPU 的感知相似度容差。

### 15.5 阶段门

- 授权 WebGL 基准入口、Start、Next 和首个 Drag 正常。
- 模型、纹理、字体和音频无本地 404。
- Canvas 不是空白且存在有效帧变化。
- 快速验证在适用页面上小于 30 秒。
- 深度验证失败不会撤销已经可用的 READY 结果，只更新为 PARTIAL。
- 报告能定位具体阶段、请求和错误。

## 16. P6 安全、可靠性和性能

日期：2026-09-28 至 2026-10-02

### 16.1 阶段目标

证明产品能够安全处理恶意或异常页面，并满足公开 Beta 的可靠性和性能门。

### 16.2 安全任务

| 编号   | 任务                             | 验证目标                     |
| ------ | -------------------------------- | ---------------------------- |
| P6-T01 | 完成正式威胁模型评审             | 所有高风险项有控制措施       |
| P6-T02 | 防止目录穿越和 Zip Slip          | 无镜像目录外写入             |
| P6-T03 | 隔离预览页面和控制 API           | 页面无法控制辅助程序         |
| P6-T04 | 校验 Native Messaging 消息       | 拒绝非法或过大消息           |
| P6-T05 | 防止任意 URL 和 SSRF             | 只处理任务观察到的资源       |
| P6-T06 | 校验重定向目标                   | 阻止危险协议和未授权本地地址 |
| P6-T07 | 过滤 Cookie、Token 和表单值      | 输出无敏感凭据               |
| P6-T08 | 限制本地服务器 Origin 和访问令牌 | 外部页面无法调用             |
| P6-T09 | 生成依赖和许可证清单             | SBOM 草案                    |
| P6-T10 | 准备安装包代码签名               | 签名构建                     |

### 16.3 可靠性任务

- 浏览器关闭或标签页消失。
- `debugger` 被其他工具占用。
- 辅助程序崩溃和自动重启。
- Native Messaging 中断。
- 网络断开和恢复。
- 磁盘空间不足。
- 文件被占用或权限不足。
- 下载中断和重试。
- 用户重复点击和重复启动。
- 系统关机后的中断任务识别。

### 16.4 性能任务

- 建立冷缓存和热缓存两套测试。
- 对每个阶段记录 P50、P90 和 P99。
- 使用 CPU、内存、磁盘和网络 Profile 定位瓶颈。
- 优先优化关键路径中占比最高的阶段。
- 验证 8 至 16 并发区间的最优点。
- 验证浏览器池预热对快速验证的收益。
- 验证 AST 解析是否应按文件大小或类型延迟执行。
- 验证 ZIP 异步化是否减少 READY 时间。

### 16.5 阶段门

- 适用页面 P50 小于 60 秒。
- 适用页面 P90 小于 120 秒。
- 50MB 任务峰值内存小于 1GB。
- 高风险安全项全部关闭或有明确接受记录。
- 恶意 Fixture 无目录外读取、写入或辅助程序控制。
- 崩溃和断线不会生成错误的 COMPLETE 状态。
- Chrome 和 Edge 当前稳定版均通过核心 E2E。

## 17. P7 发布准备与公开 Beta

日期：2026-10-05 至 2026-10-09

### 17.1 发布任务

| 编号   | 任务                                | 产出           |
| ------ | ----------------------------------- | -------------- |
| P7-T01 | 生成 Chrome 和 Edge 发布构建        | 商店包         |
| P7-T02 | 完成辅助程序安装、更新和卸载        | 签名安装包     |
| P7-T03 | 建立 Dev、Canary、Beta、Stable 渠道 | 发布通道       |
| P7-T04 | 完成隐私政策和使用条款              | 合规文档       |
| P7-T05 | 完成权限用途和审核说明              | 商店材料       |
| P7-T06 | 完成用户安装和故障排查文档          | 帮助文档       |
| P7-T07 | 建立诊断包导出                      | 支持工具       |
| P7-T08 | 建立版本回滚方案                    | 回滚 Runbook   |
| P7-T09 | 建立 Beta 反馈和缺陷分级流程        | 支持流程       |
| P7-T10 | 执行发布候选回归测试                | Release Report |

### 17.2 Beta 发布门槛

- 20 个基准页面至少 18 个可运行。
- 适用页面 P90 小于 120 秒。
- 完整结果本地资源 404 为 0。
- 输出中不存在 Cookie、Token、密码和表单值。
- Chrome 和 Edge 核心 E2E 通过。
- 安装、更新、卸载和回滚通过。
- 已知 P0 和 P1 缺陷为 0。
- P2 缺陷有明确影响说明和修复计划。

## 18. P8 Beta 硬化与进一步优化

日期：2026-10-12 至 2026-11-20

### 18.1 样本扩展

- 收集至少 100 个获得授权的真实页面。
- 覆盖静态营销页、SPA、WebGL、音频、Worker、iframe 和公共 CDN。
- 按资源数量、总体积、框架、站点类型和失败原因分层统计。
- 不把同一技术栈的重复页面当作独立覆盖。

### 18.2 优化闭环

每个优化项必须遵循：

1. 从指标或用户报告定位问题。
2. 创建可复现 Fixture。
3. 记录修改前基线。
4. 实现最小修复。
5. 运行正确性、安全和性能回归。
6. 记录修改后指标。
7. 决定是否推广到通用路径。

### 18.3 优化优先级

第一优先级：

- 镜像无法运行。
- 敏感数据风险。
- 本地资源缺失。
- 崩溃和任务状态错误。

第二优先级：

- P90 超过 120 秒。
- 内存和磁盘异常增长。
- 重试和缓存效率低。
- Chrome 与 Edge 行为差异。

第三优先级：

- UI 细节。
- 报告样式。
- 非关键格式支持。
- 高级设置。

### 18.4 预计优化方向

- 动态调整发现静默窗口。
- 按首屏关键性设置资源优先级。
- 使用浏览器已有响应减少二次下载。
- 对公共库和大型模型提高缓存命中。
- 小文件批量写入和 SQLite 事务合并。
- 大文件流式 Hash 和零拷贝路径。
- 根据 429、503 和吞吐自动调节并发。
- 延迟解析非关键大型 JavaScript。
- 复用 Playwright Browser 和 Context。
- 对 WebGL 页面采用场景级验证，而非全页逐像素验证。
- 建立失败模式到站点适配器的最小扩展机制。

### 18.5 正式发布候选门槛

- 100 个授权页面中，适用范围成功率至少 95%。
- P50 小于 60 秒，P90 小于 120 秒。
- 完整结果本地资源 404 为 0。
- 意外访问原站静态资源的完整结果为 0。
- 无未处理的高风险安全问题。
- 连续两周没有新的 P0 缺陷。
- 崩溃率和中断任务率达到内部阈值。
- 安装和升级成功率达到内部阈值。

## 19. 测试战略

### 19.1 测试层次

| 层次     | 目标           | 主要内容                            |
| -------- | -------------- | ----------------------------------- |
| 单元测试 | 验证纯逻辑     | URL、状态机、解析、过滤、Hash、评分 |
| 协议测试 | 验证跨进程契约 | Schema、版本协商、错误码、重连      |
| 集成测试 | 验证模块组合   | CDP、下载、重写、服务、缓存         |
| E2E 测试 | 验证用户流程   | 一键抓取、取消、重试、结果页        |
| 离线测试 | 验证核心价值   | 断网运行、无原站请求、无 404        |
| 视觉测试 | 验证高保真     | 截图、Canvas、动态容差              |
| 性能测试 | 验证时间预算   | 冷热缓存、不同体积和请求数          |
| 安全测试 | 验证隔离       | 路径、SSRF、消息、控制 API、凭据    |

不以单一总覆盖率作为质量结论，但以下关键模块的分支覆盖率不得低于 85%：

- 任务状态机。
- URL 规范化和安全路径映射。
- HTML、CSS、JSON 和 JavaScript 资源发现。
- 敏感 Header 和日志脱敏。
- 下载重试、取消和恢复。
- Native Messaging Schema 校验。
- 完整性状态判定。

### 19.2 Fixture 矩阵

必须具备可本地运行的 Fixture：

1. 纯 HTML、CSS 和图片。
2. 多级 CSS import 和字体。
3. 带 srcset、preload 和懒加载的长页面。
4. History API SPA。
5. iframe 和跨 Target Worker。
6. Service Worker 缓存。
7. WebSocket 和 EventSource 长连接。
8. GLB、DDS、BIN 和音频 WebGL 页面。
9. WASM 和 Worker 页面。
10. 重定向、403、404、超时和 429。
11. 大量小文件页面。
12. 单个大文件页面。
13. 路径穿越和恶意 URL 页面。
14. 包含 Cookie、Authorization 和表单数据的页面。
15. 绝对 URL 和公共 CDN 页面。

### 19.3 基准测试集

Beta 前 20 个基准页面建议分布：

| 类型            | 数量 |
| --------------- | ---: |
| 普通静态页面    |    5 |
| 静态 SPA        |    4 |
| 长页面和懒加载  |    3 |
| WebGL/Canvas    |    4 |
| Worker/WASM     |    2 |
| iframe/CDN 组合 |    2 |

### 19.4 回归策略

- 每次 PR 运行单元、协议和关键集成测试。
- 每日运行 Chrome 完整 E2E。
- 每日运行 5 个核心基准页面。
- 每周运行 Chrome 和 Edge 全矩阵。
- Release Candidate 必须覆盖 Chrome 和 Edge 当前稳定版及前两个主要版本。
- Windows 10 和 Windows 11 必须分别完成安装、抓取、升级和卸载验证。
- Release Candidate 必须运行全部安全、性能和离线测试。

## 20. 性能工程计划

### 20.1 时间预算

各阶段预算不是简单相加，发现和下载应并行。

| 阶段           | P50 预算 | P90 预算 |
| -------------- | -------: | -------: |
| 预检查和接入   |     2 秒 |     5 秒 |
| 页面刷新和发现 |    10 秒 |    25 秒 |
| 资源下载       |    20 秒 |    55 秒 |
| 本地化处理     |     5 秒 |    12 秒 |
| 本地启动       |     5 秒 |    10 秒 |
| 快速验证       |    15 秒 |    30 秒 |
| 端到端 READY   |    60 秒 |   120 秒 |

### 20.2 必须采集的埋点

- `preflight_ms`
- `attach_ms`
- `page_reload_ms`
- `discovery_ms`
- `download_ms`
- `download_bytes`
- `download_effective_mbps`
- `rewrite_ms`
- `preview_start_ms`
- `fast_validation_ms`
- `ready_ms`
- `peak_memory_mb`
- `asset_count`
- `cache_hit_count`
- `retry_count`
- `missing_asset_count`
- `unexpected_remote_request_count`

### 20.3 性能优化顺序

1. 消除不必要的串行等待。
2. 避免同一资源重复下载。
3. 流式下载、Hash 和落盘。
4. 优先处理首屏和主程序资源。
5. 减少 SQLite 事务和小文件同步写。
6. 延迟非关键解析、ZIP 和深度验证。
7. 复用浏览器、进程和本地服务。
8. 最后才考虑更换运行语言或大规模重写。

### 20.4 性能回归规则

- READY P50 增加超过 10% 时必须说明。
- READY P90 增加超过 5% 时必须阻止发布，除非修复了更高优先级安全或正确性问题。
- 内存增加超过 100MB 时必须提供 Profile。
- 新增资源类型支持必须加入独立基准页面。

## 21. 安全与隐私计划

### 21.1 信任模型

以下内容全部视为不可信：

- 来源页面。
- 页面脚本和 Worker。
- URL、文件名和 Header。
- 下载的 HTML、JSON、模型和压缩资源。
- 页面产生的 Native Messaging 内容。
- 镜像运行时发起的请求。

### 21.2 主要威胁

- 页面诱导扩展抓取未授权数据。
- URL 或文件名导致路径穿越。
- 本地辅助程序被用作 SSRF 或内网扫描。
- 预览页面调用本地控制 API。
- 恶意页面读取本机文件。
- Native Messaging 发送超大或畸形消息。
- 日志和报告泄漏 Token。
- ZIP 导出产生 Zip Slip。
- 镜像页面冒充原站继续提交表单。

### 21.3 控制措施

- 只允许用户主动点击后启动。
- 只处理当前标签页观察到的资源。
- 默认拒绝 `file:`、`chrome:` 和危险协议。
- 默认不支持私网和本机地址抓取。
- 重定向后重新执行地址和协议校验。
- 所有输出路径使用安全映射，不使用原始 URL 直接拼接。
- 控制 API 使用独立端口和任务令牌。
- 预览服务只提供镜像文件，不提供系统命令。
- 默认禁用或标记表单提交和写接口。
- 日志和报告统一使用脱敏函数。
- 任务目录使用最小权限。
- 构建生成 SBOM，安装包进行代码签名。

### 21.4 安全质量门

- 所有高风险项在 Beta 前关闭。
- 未关闭的中风险项需要负责人、期限和接受记录。
- 凭据泄漏测试必须自动化。
- 安全 Fixture 每周运行。
- 发布候选必须执行独立安全评审。

## 22. 可观测性与诊断

### 22.1 本地日志

- 使用结构化 JSON 日志。
- 每个任务有唯一关联 ID。
- 记录阶段开始、结束、耗时和错误码。
- 默认不记录页面正文和完整敏感 URL。
- 用户可主动导出诊断包。

### 22.2 Beta 遥测

遥测默认应最小化并明确告知，建议只收集：

- 匿名版本和平台信息。
- 任务成功、部分、失败状态。
- 阶段耗时分桶。
- 资源数和体积分桶。
- 错误码。
- 崩溃信号。

默认不收集：

- 页面内容。
- 完整 URL。
- Cookie 和 Token。
- 截图。
- 本地路径。

### 22.3 告警和分析

- P90 连续三天高于 120 秒时触发性能调查。
- 某错误码占任务 5% 以上时创建专项问题。
- 新版本成功率下降 3 个百分点时暂停扩大发布。
- 出现敏感数据事件时立即停止相关版本发布。

## 23. 发布和版本管理

### 23.1 发布通道

| 通道   | 用户     | 目的         |
| ------ | -------- | ------------ |
| Dev    | 开发人员 | 日常调试     |
| Canary | 内部测试 | 每日集成     |
| Beta   | 授权用户 | 真实样本验证 |
| Stable | 公开用户 | 正式发布     |

### 23.2 版本兼容

- 扩展和辅助程序独立版本化。
- 协议使用独立版本号。
- 扩展启动时先协商协议版本。
- 不兼容时给出升级路径，不继续执行任务。
- 任务清单包含生成它的所有组件版本。

### 23.3 构建要求

- CI 中产生可追踪构建。
- 发布包生成 SHA-256。
- 安装包和扩展包保留构建来源记录。
- 依赖版本锁定。
- 生成 SBOM。
- 发布必须由双人批准。

### 23.4 回滚

- 保留上一稳定版辅助程序安装包。
- 扩展新版本出现高风险问题时停止分阶段发布。
- 数据库 Schema 变更必须可向前兼容或具备迁移回滚。
- 镜像输出格式使用版本字段，避免旧任务无法打开。

## 24. 风险登记表

| 风险                      | 概率 | 影响 | 早期信号             | 缓解措施                          | 负责人     |
| ------------------------- | ---- | ---- | -------------------- | --------------------------------- | ---------- |
| CDP 资源事件缺失          | 中   | 高   | Fixture 召回率下降   | 多通道发现、事件归一化            | CAP        |
| 压缩 JS 无法安全重写      | 高   | 高   | 本地仍访问原站       | 保持目录、AST 定向替换、依赖报告  | MIR        |
| WebGL GPU 差异            | 高   | 中   | 视觉测试波动         | 感知对比、Canvas 检查、同环境基线 | VAL        |
| Native Messaging 安装摩擦 | 中   | 高   | 连接失败率高         | 安装检测、自动修复、清晰引导      | EXT/SEC    |
| 辅助程序成为 SSRF 工具    | 中   | 高   | 非任务 URL 请求      | 观察证据、协议和地址校验          | SEC        |
| 资源体积导致超过两分钟    | 高   | 中   | download_ms 占比高   | 预检查、并发、缓存、部分完成      | MIR        |
| 商店对权限审核严格        | 中   | 高   | 审核反馈或延期       | 最小权限、单一用途、材料提前准备  | PX/SEC     |
| 用户误认为复制后端        | 高   | 中   | 支持请求集中         | 预检查和结果报告明确边界          | PX         |
| 测试样本技术栈单一        | 中   | 高   | Beta 失败模式集中    | 分层基准和授权样本池              | QA         |
| 项目范围扩张              | 高   | 高   | 新增多页面或工程重构 | 功能冻结和变更控制                | 技术负责人 |

## 25. 缺陷分级

| 级别 | 定义                                       | 处理要求      |
| ---- | ------------------------------------------ | ------------- |
| P0   | 凭据泄漏、任意文件访问、广泛数据破坏       | 立即停止发布  |
| P1   | 核心流程不可用、镜像错误标记完整、普遍崩溃 | 当前阶段修复  |
| P2   | 部分页面失败、性能明显下降、报告不准确     | Beta 前有计划 |
| P3   | 非关键体验、样式和低频兼容问题             | 排入后续迭代  |

## 26. 变更控制

任何新增需求必须回答：

1. 是否属于当前 PRD 的 MVP 范围。
2. 是否增加敏感权限或数据处理。
3. 是否影响 P90 120 秒目标。
4. 是否增加本地辅助程序安装复杂度。
5. 是否需要新的测试 Fixture。
6. 将替换哪个现有任务或延长多少时间。

以下变更必须由产品和技术负责人共同批准：

- 新增浏览器平台。
- 新增云端服务。
- 支持登录态页面。
- 保存 Cookie 或浏览器存储。
- 多页面递归。
- 工程重构。
- 改变本地服务器安全边界。

## 27. 阶段性 Go/No-Go 检查

### 27.1 POC Go

- 简单页面和 WebGL 首场景均可离线运行。
- 架构没有不可接受的安全阻塞。

### 27.2 Alpha Go

- 10 个 Fixture 通过端到端离线测试。
- 任务状态、取消和失败行为稳定。

### 27.3 功能完成 Go

- PRD P0 功能全部实现。
- 用户流程可用。
- 不再增加功能范围。

### 27.4 Beta Go

- 20 个基准页面至少 18 个成功。
- P90 小于 120 秒。
- P0/P1 缺陷为 0。
- 安全和安装流程通过。

### 27.5 Stable Go

- 100 个授权页面成功率至少 95%。
- 连续两周无新 P0。
- 核心指标稳定。
- 回滚和支持流程已演练。

## 28. 首十个工作日执行清单

### 2026-07-20，工作日 1

- 初始化 Monorepo。
- 建立 TypeScript、Lint、测试和 CI。
- 创建 ADR 模板和风险登记表。
- 确认目标浏览器和测试机。

### 2026-07-21，工作日 2

- 创建第一批本地 Fixture。
- 建立 MV3 扩展骨架。
- 验证当前标签页权限和 `chrome.debugger` attach。

### 2026-07-22，工作日 3

- 记录页面刷新网络事件。
- 生成第一版资源清单。
- 处理重定向、失败和缓存命中。

### 2026-07-23，工作日 4

- 建立辅助程序骨架。
- 打通 Native Messaging 握手、心跳和版本信息。
- 完成最小公共资源下载器。

### 2026-07-24，工作日 5

- 启动本地 HTTP 服务。
- 完成简单页面断网重放。
- 对 WebGL 基准进行第一次离线尝试。
- 召开 P0 阶段门评审。

### 2026-07-27，工作日 6

- 实现版本化协议 Schema。
- 实现任务 ID 和状态机。
- 建立扩展和辅助程序结构化日志。

### 2026-07-28，工作日 7

- 实现一键创建任务。
- 实现任务进度事件。
- 实现辅助程序缺失和版本不兼容提示。

### 2026-07-29，工作日 8

- 实现下载队列和流式落盘。
- 实现 SHA-256。
- 建立临时任务目录和原子完成策略。

### 2026-07-30，工作日 9

- 实现 HTML 和 CSS 基础重写。
- 实现本地预览入口。
- 增加断网 E2E。

### 2026-07-31，工作日 10

- 完成第一次端到端演示。
- 统计各阶段耗时。
- 更新 ADR、风险和后续任务优先级。
- 决定是否按原架构进入完整实现。

## 29. 下一版本优化候选

Beta 稳定后按数据决定，不提前进入 MVP：

- 同域多页面队列。
- macOS 辅助程序。
- 站点适配器 SDK。
- 更完整的 Service Worker 离线重放。
- WARC 或标准归档格式导出。
- 可选的代码格式化和工程整理。
- 团队镜像库和版本对比。
- 云端同步和远程验证。
- 更大规模的浏览器兼容矩阵。

## 30. 计划完成标准

本项目不以“代码写完”作为完成标准。正式发布候选必须同时满足：

- 产品流程可用。
- 镜像结果可离线运行。
- 质量指标达到门槛。
- 安全和隐私边界经过验证。
- 安装、升级、回滚和支持流程可执行。
- 有足够真实样本证明方案不是只对单一网站有效。
- 后续优化可以通过指标和 Fixture 持续迭代。
